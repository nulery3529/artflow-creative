// Removed temporary setup probe. This file is intentionally left as a non-route placeholder during cleanup.
export default function handler(req,res){ return res.status(404).json({error:'Not found'}); }
